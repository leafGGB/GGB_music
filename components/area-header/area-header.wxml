<!--components/video-item/area-header/area-header.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view class="more">
    <text class="text" wx:if="{{hasMore}}" bindtap="onMoreTap">更多</text>
    <van-icon name="arrow" wx:if="{{hasMore}}" />
  </view>
</view>
